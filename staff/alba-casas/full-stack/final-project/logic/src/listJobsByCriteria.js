function listJobsByCriteria(criteria) {
    // TODO validate criteria (object)

    // TODO filter by possible and optional criteria fields
    /*
    criteria.city (exact match)
    criteria.role (exact match)
    criteria.query (cootains)

    Job.find({ ... })
    */
}